#!/usr/bin/node

// tableau des langages
const languages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// boucle pour parcourir le tableau
for (let i = 0; i < languages.length; i++) {
  console.log(languages[i]); // affiche chaque élément
}
